name: Daily Scrape

on:
  schedule:
    - cron: '0 3 * * *'   # 3am UTC daily
  workflow_dispatch: {}    # lets you trigger manually too

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run scraper
        env:
          BRIGHTDATA_API_TOKEN: ${{ secrets.BRIGHTDATA_API_TOKEN }}
        run: |
          npx -p @brightdata/cli bdata scraper run c_mt5evkhh7xjyn2fi0 https://www.instahyre.com/search --pretty > jobs.json

      - name: Update database
        run: python pipeline.py

      - name: Commit updated db
        run: |
          git config user.name "indiser"
          git config user.email "banerjeerana74@gmail.com"
          git add .
          git commit -m "Daily scrape update" || echo "No changes"
          git push
